# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'CryptoCurrency_v2.0' do
  # Comment the next line if you don't want to use dynamic frameworks
  #use_frameworks!
  use_modular_headers!

  # Pods for CryptoCurrency_v2.0
  pod 'Alamofire', '~> 5.1'
  pod 'SwiftyJSON', '~> 4.0'

end
